<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.mall">
	<resultMap id="proResult" type="productVO">
		<result property="pseq" column="pseq" />
		<result property="name" column="name" />
		<result property="kind" column="kind" />
		<result property="price1" column="price1" />
		<result property="price2" column="price2" />
		<result property="price3" column="price3" />
		<result property="content" column="content" />
		<result property="image" column="image" />
		<result property="useyn" column="useyn" />
		<result property="bestyn" column="bestyn" />
		<result property="indate" column="indate" />

	</resultMap>

	<!-- 새 상품 -->
	<select id="newProduct" resultMap="proResult">
      <![CDATA[
     	select *  from new_pro_view
      ]]>
	</select>

	<!-- 베스트 상품 -->
	<select id="bestProduct" resultMap="proResult">
      <![CDATA[
     	select *  from best_pro_view
      ]]>
	</select>

	<!-- 상품 번호 가져오기 -->
	<select id="selectPseq" resultMap="proResult">
      <![CDATA[
     	select * from product where pseq=?
      ]]>
	</select>

	<!-- 상품 카테고리 -->
	<select id="selectKind" resultMap="proResult">
      <![CDATA[
     	select * from product where kind=?
      ]]>
	</select>

</mapper>